# 🎨 Frontend-Backend Integration Architecture

## 📊 Complete System Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (React + Vite)                        │
│                     http://localhost:5173 (dev)                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                    Dashboard.tsx                                │   │
│  │  ┌──────────────────────────────────────────────────────────┐  │   │
│  │  │           ChatInterface.tsx (Main UI)                    │  │   │
│  │  │                                                          │  │   │
│  │  │  User Input: "https://github.com/user/repo"            │  │   │
│  │  │       │                                                  │  │   │
│  │  │       ▼                                                  │  │   │
│  │  │  extractGitHubUrl() ─────────────────────────┐         │  │   │
│  │  │       │                                       │         │  │   │
│  │  │       ▼                                       │         │  │   │
│  │  │  Detect Analysis Type                        │         │  │   │
│  │  │  (SHORT/MEDIUM/HARD)                         │         │  │   │
│  │  │       │                                       │         │  │   │
│  │  │       ▼                                       │         │  │   │
│  │  └───────┼───────────────────────────────────────┼─────────┘  │   │
│  │          │                                       │            │   │
│  └──────────┼───────────────────────────────────────┼────────────┘   │
│             │                                       │                │
│             ▼                                       │                │
│  ┌─────────────────────────────────────────────────┼───────────┐    │
│  │              services/api.ts                    │           │    │
│  │                                                  ▼           │    │
│  │  ┌────────────────────┐     ┌────────────────────────────┐ │    │
│  │  │  REST API Calls    │     │  WebSocket Manager         │ │    │
│  │  │                    │     │                            │ │    │
│  │  │ • healthCheck()    │     │ • connectToAnalysis()      │ │    │
│  │  │ • createAnalysis() │     │ • Socket Events:           │ │    │
│  │  │ • getAnalysis()    │     │   - connect                │ │    │
│  │  │ • getResults()     │     │   - progress_update        │ │    │
│  │  │ • listAnalyses()   │     │   - intermediate_result    │ │    │
│  │  │                    │     │   - analysis_complete      │ │    │
│  │  │                    │     │   - error                  │ │    │
│  │  └────────┬───────────┘     └──────────┬─────────────────┘ │    │
│  │           │                            │                   │    │
│  └───────────┼────────────────────────────┼───────────────────┘    │
│              │                            │                        │
└──────────────┼────────────────────────────┼────────────────────────┘
               │                            │
               │ HTTP REST                  │ WebSocket
               │ (JSON)                     │ (Real-time)
               │                            │
               ▼                            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                 BACKEND (Flask + Flask-SocketIO)                        │
│               http://140.238.227.29:5000                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────┐    ┌──────────────────────────┐ │
│  │      REST API Routes             │    │   WebSocket Handler      │ │
│  │      (app/routes/api.py)         │    │   (socketio_events.py)   │ │
│  │                                  │    │                          │ │
│  │  POST   /api/analysis            │    │  Namespace: /analysis    │ │
│  │  GET    /api/analysis/:id        │    │                          │ │
│  │  GET    /api/analysis/:id/results│    │  Events:                 │ │
│  │  GET    /api/analyses            │    │  • start_analysis        │ │
│  │  GET    /api/health              │    │  • progress_update       │ │
│  │                                  │    │  • analysis_complete     │ │
│  └──────────────┬───────────────────┘    └──────────┬───────────────┘ │
│                 │                                    │                 │
│                 ▼                                    │                 │
│  ┌──────────────────────────────────────────────────┼────────────┐    │
│  │           Analysis Orchestrator                  │            │    │
│  │     (services/analysis_orchestrator.py)          │            │    │
│  │                                                   ▼            │    │
│  │  ┌────────────────────────────────────────────────────────┐  │    │
│  │  │         Autonomous Agent Pipeline                      │  │    │
│  │  │                                                         │  │    │
│  │  │  1. Clone Repo       (0-20%)    ──▶ emit progress     │  │    │
│  │  │  2. Chunk Code       (20-35%)   ──▶ emit progress     │  │    │
│  │  │  3. Generate Embed   (35-55%)   ──▶ emit progress     │  │    │
│  │  │  4. Search CVE       (55-75%)   ──▶ emit progress     │  │    │
│  │  │                                  ──▶ emit findings     │  │    │
│  │  │  5. Validate (GPT-4) (75-95%)   ──▶ emit progress     │  │    │
│  │  │  6. Generate Report  (95-100%)  ──▶ emit complete     │  │    │
│  │  │                                                         │  │    │
│  │  └─────────────────────────────────────────────────────────┘  │    │
│  │                                                                │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                      Services Layer                            │   │
│  │                                                                │   │
│  │  • repo_service.py        - Clone & parse repositories        │   │
│  │  • chunking_service.py    - Code chunking                     │   │
│  │  • embedding_service.py   - Cohere embeddings                 │   │
│  │  • cohere_service.py      - Cohere API client                 │   │
│  │  • cve_search_service.py  - FAISS vector search               │   │
│  │  • faiss_manager.py       - FAISS index management            │   │
│  │  • validation_service.py  - GPT-4 validation                  │   │
│  │  • report_service.py      - Report generation                 │   │
│  │                                                                │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                      Data Layer                                │   │
│  │                                                                │   │
│  │  • SQLite Database                                             │   │
│  │    - Analysis          (analysis records)                      │   │
│  │    - CodeChunk         (parsed code chunks)                    │   │
│  │    - CVEFinding        (vulnerability findings)                │   │
│  │    - CVEDataset        (CVE database)                          │   │
│  │                                                                │   │
│  │  • FAISS Vector Store                                          │   │
│  │    - CVE embeddings for semantic search                        │   │
│  │                                                                │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
               │                                    │
               ▼                                    ▼
┌──────────────────────────┐        ┌────────────────────────────┐
│    External APIs         │        │    File System             │
│                          │        │                            │
│  • Cohere API            │        │  • Cloned Repositories     │
│    - Embeddings          │        │  • FAISS Indexes           │
│    - Reranking           │        │  • Generated Reports       │
│                          │        │                            │
│  • OpenAI GPT-4          │        └────────────────────────────┘
│    - Validation          │
│    - False positive      │
│      detection           │
│                          │
└──────────────────────────┘
```

---

## 🔄 Data Flow Example

### User Action: Paste GitHub URL

```
User in ChatInterface.tsx
    │
    ├─▶ Input: "https://github.com/pallets/flask"
    │
    ▼
extractGitHubUrl(input)
    │
    ├─▶ Extracted: "https://github.com/pallets/flask"
    │
    ▼
startAnalysis(url, "MEDIUM")
    │
    ├─▶ 1. createAnalysis(url, type)
    │       │
    │       └─▶ POST http://140.238.227.29:5000/api/analysis
    │           {
    │             "repo_url": "https://github.com/pallets/flask",
    │             "analysis_type": "MEDIUM"
    │           }
    │           │
    │           ▼
    │       Response: { analysis_id: 123, status: "pending", ... }
    │
    ├─▶ 2. connectToAnalysis(123, callbacks)
    │       │
    │       └─▶ WebSocket: ws://140.238.227.29:5000/analysis
    │           │
    │           ├─▶ emit('start_analysis', { analysis_id: 123 })
    │           │
    │           ├─▶ on('analysis_started') ─▶ Show "Analysis started"
    │           │
    │           ├─▶ on('progress_update') ─▶ Update progress bar
    │           │       │
    │           │       ├─▶ 0% - cloning
    │           │       ├─▶ 20% - chunking
    │           │       ├─▶ 35% - embedding
    │           │       ├─▶ 55% - searching
    │           │       ├─▶ 75% - validating
    │           │       └─▶ 95% - finalizing
    │           │
    │           ├─▶ on('intermediate_result') ─▶ Show findings
    │           │       │
    │           │       └─▶ "Found CVE-2021-44228 (CRITICAL)"
    │           │
    │           └─▶ on('analysis_complete') ─▶ Fetch results
    │                   │
    │                   └─▶ getAnalysisResults(123)
    │                       │
    │                       └─▶ GET /api/analysis/123/results
    │                           │
    │                           ▼
    │                       Display summary & findings
    │
    ▼
Analysis Complete! Show final results
```

---

## 🎯 Component Interaction

```
┌────────────────────────────────────────────────────────────┐
│                    User Interface                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ChatInterface.tsx                                         │
│  ├─▶ Message List (shows chat history)                    │
│  ├─▶ Input Area (accepts GitHub URLs)                     │
│  └─▶ Analysis Progress (when analyzing)                   │
│       │                                                    │
│       └─▶ AnalysisProgress.tsx                            │
│            ├─▶ Stage indicators (6 stages)                │
│            ├─▶ Progress bar (0-100%)                      │
│            └─▶ Current stage description                  │
│                                                            │
└────────────────────────────────────────────────────────────┘
                            │
                            │ Uses
                            ▼
┌────────────────────────────────────────────────────────────┐
│                   API Service Layer                        │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  services/api.ts                                           │
│  ├─▶ REST API Functions                                   │
│  │    ├─▶ createAnalysis(url, type)                       │
│  │    ├─▶ getAnalysis(id)                                 │
│  │    ├─▶ getAnalysisResults(id)                          │
│  │    └─▶ listAnalyses(page, perPage, status)             │
│  │                                                         │
│  ├─▶ WebSocket Manager                                    │
│  │    ├─▶ connectToAnalysis(id, callbacks)                │
│  │    └─▶ disconnectSocket(socket)                        │
│  │                                                         │
│  └─▶ Helper Functions                                     │
│       ├─▶ extractGitHubUrl(text)                          │
│       ├─▶ getSeverityColor(severity)                      │
│       └─▶ formatDuration(seconds)                         │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security & State Management

```
Frontend State:
┌──────────────────────────────────────────────────┐
│  const [messages, setMessages] = useState([])    │
│  const [isAnalyzing, setIsAnalyzing] = useState  │
│  const [analysisState, setAnalysisState] =       │
│    useState({ id, progress, stage, status })     │
│  const [currentSocket, setCurrentSocket] =       │
│    useState(null)                                │
└──────────────────────────────────────────────────┘
                    │
                    │ Real-time updates via WebSocket
                    ▼
Backend State:
┌──────────────────────────────────────────────────┐
│  Database (SQLite):                              │
│  ├─▶ Analysis Table                              │
│  │    └─▶ status: pending → running → completed  │
│  │                                                │
│  ├─▶ CodeChunk Table                             │
│  │    └─▶ Parsed code segments                   │
│  │                                                │
│  └─▶ CVEFinding Table                            │
│       └─▶ Discovered vulnerabilities             │
└──────────────────────────────────────────────────┘
```

---

## 📊 Message Flow Timeline

```
Time    Frontend                  Backend                   External
─────────────────────────────────────────────────────────────────────
0:00    User pastes URL
        │
0:01    extractGitHubUrl()
        │
0:02    createAnalysis() ────▶    Create DB record
        │                          status: "pending"
0:03    connectToAnalysis()        │
        WebSocket connect    ◀────┘
        │
0:04    emit(start_analysis) ─▶    Background task starts
        │                          status: "running"
0:05    ◀──────────────────── emit(analysis_started)
        Show "Started..."
        │
0:30    ◀──────────────────── emit(progress: 15%)
        Update progress bar        Cloning repo ──────▶  GitHub
        │
1:00    ◀──────────────────── emit(progress: 30%)
        Update progress bar        Chunking code
        │
2:00    ◀──────────────────── emit(progress: 45%)
        Update progress bar        Generate embeddings ▶  Cohere API
        │
3:00    ◀──────────────────── emit(progress: 65%)
        Update progress bar        Search CVE database
        │
3:30    ◀──────────────────── emit(intermediate_result)
        Show finding               Found CVE-2021-44228
        │
4:00    ◀──────────────────── emit(progress: 85%)
        Update progress bar        Validate with AI ───▶  OpenAI GPT-4
        │
5:00    ◀──────────────────── emit(progress: 100%)
        │                          status: "completed"
5:01    ◀──────────────────── emit(analysis_complete)
        │
5:02    getAnalysisResults() ─▶    Fetch from DB
        │                          Return summary + findings
5:03    ◀──────────────────── Return results JSON
        │
5:04    Display final summary
        Disconnect socket
```

---

## 🎨 UI State Transitions

```
┌──────────────┐
│   Initial    │  Welcome message
│     State    │  3 quick action cards
└──────┬───────┘
       │ User sends GitHub URL
       ▼
┌──────────────┐
│   Analysis   │  "🚀 Analysis started..."
│   Starting   │  "Analysis Type: MEDIUM"
└──────┬───────┘
       │ WebSocket connected
       ▼
┌──────────────┐
│   Analysis   │  Progress Bar Component:
│   Running    │  • Stage indicators
│              │  • Progress: 0% → 100%
│              │  • Live stage descriptions
└──────┬───────┘
       │ Findings discovered
       ▼
┌──────────────┐
│  Showing     │  "🔍 Found CVE-..."
│  Findings    │  Real-time vulnerability cards
└──────┬───────┘
       │ Analysis complete
       ▼
┌──────────────┐
│   Complete   │  "🎉 Analysis Complete!"
│   Summary    │  • Statistics
│              │  • Severity breakdown
│              │  • Duration
└──────────────┘
```

---

## 🛠️ Technology Stack

### Frontend:
- **Framework:** React 18 + TypeScript
- **Build Tool:** Vite
- **UI Library:** Shadcn/ui + Tailwind CSS
- **WebSocket:** socket.io-client
- **HTTP Client:** Fetch API
- **State Management:** React hooks (useState, useEffect)
- **Routing:** React Router DOM

### Backend:
- **Framework:** Flask (Python)
- **WebSocket:** Flask-SocketIO
- **Database:** SQLite + SQLAlchemy
- **Vector Search:** FAISS
- **Embeddings:** Cohere API
- **AI Validation:** OpenAI GPT-4
- **Code Parsing:** tree-sitter

---

## 📈 Performance Considerations

```
Analysis Type    Duration    Files      Validation    Recommended For
─────────────────────────────────────────────────────────────────────
SHORT            2-3 min     ≤ 500      No            Quick checks
MEDIUM           5-10 min    ≤ 2000     Yes ✓         Most cases ⭐
HARD             15-40 min   Unlimited  Yes ✓         Large projects
```

---

## ✅ Integration Checklist

- [x] API service with TypeScript types
- [x] WebSocket connection manager
- [x] ChatInterface with URL detection
- [x] Real-time progress updates
- [x] Dynamic AnalysisProgress component
- [x] Error handling & user feedback
- [x] Socket cleanup on unmount
- [x] Message streaming to chat
- [x] Severity color coding
- [x] Duration formatting
- [x] GitHub URL extraction
- [x] Analysis type selection
- [x] Comprehensive documentation
- [x] Test guides & examples

---

**System is fully integrated and ready for production! 🚀**
